# Data Folder

This folder is the athlete memory layer.

Private files such as `training_history.json`, `race_history.json`, sleep references, body metrics, and physiology tests should stay local unless they are explicitly sanitized.

Use the `sample_*.json` files as public examples of the expected shape.
